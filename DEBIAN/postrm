#!/bin/bash
set -e

case "$1" in
  purge)
    # Do not remove /etc/nbfc/nbfc.json by default — user may still use nbfc.
    # GRUB backups left intentionally.
    rm -f /etc/sudoers.d/nitro-control
    ;;
esac

exit 0
